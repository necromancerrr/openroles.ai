{
  "name": "openroles",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.6.0"
  },
  "description": "Internship & new-grad tracker — the Ledger design system. A scanning surface where age is the only thing that gets color.",
  "scripts": {
    "dev": "next dev",
    "prebuild": "node --experimental-strip-types scripts/fetch-logos.ts --budget=300 || echo '[prebuild] logo fetch skipped — building without it'",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "ingest:sources": "node --experimental-strip-types scripts/generate-sources.ts",
    "eval:classifier": "node --experimental-strip-types scripts/eval-classifier.ts",
    "audit:logos": "node --experimental-strip-types scripts/audit-logo-domains.ts",
    "fetch:logos": "node --experimental-strip-types scripts/fetch-logos.ts"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "^16.2.11",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "22.10.7",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "typescript": "5.7.3"
  }
}
